---
title: Quiz
---
import { TypeTable } from 'fumadocs-ui/components/type-table';
import Link from 'fumadocs-core/link';

## Props
Quiz can be typed as `QuizProps` which contains all of the following fields.
<TypeTable
  type={{
    content: {
      type: 'NonEmptyArray<MixedContent>',
    },
    className: {
      type: 'React.CSSProperties',
      description: '(Optional) Additional customized class names to be applied to the component.',
      default: 'null'
    },
    style: {
      type: 'React.CSSProperties',
      description: '(Optional) Additional customized styles to be applied to the component.',
      default: 'null'
    },
    theme: {
      type: '"light" | "dark"',
    },
    shuffle: {
      description: '(Optional) Whether to randomize the order of the questions.',
      type: 'boolean',
      default: 'false'
    }
  }}
/>

## More information

- `content`: Refer to <Link href="/docs/types/MixedContent">`MixedContent`</Link>
- `className`: (Optional) Additional customized class names to be applied to the component.
- `style`: (Optional) Additional customized styles to be applied to the component.
- `theme`: Sets the color scheme of component.
- `shuffle`: (Optional) Whether to randomize the order of the questions.